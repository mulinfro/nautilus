ss = "qwer" + 'aswde' +  """ dasf' " ds 
qq love"""
l = True if len(ss) > 1 else False

lst = [1,3,7,2,4,6,0, 2:16:3, 1:14]

1 + 2*3/4%5


a = w=d =ss


osduc= $ dir
def print_test()
    print("hello world")
    for (i in [1:10])
        print("i:", i, i*i)
    end
end


def qsort(lst)
    lt = lambda (x,y): x<y
    gt = lambda (x,y): x>y
    eq = lambda (x,y): x==y
    if (len(lst) <= 1)
        return lst
    end
    leftpart = qsort(filter(lt(_, lst[0]), lst) )
    rightpart = qsort(filter(gt(_, lst[0]), lst) )
    eqpart = filter(eq(_, lst[0]), lst)
    return  leftpart + eqpart + rightpart
end
